# WebApplication_HW4
Look at backend README to start the backend from backend terminal
Look at frontend README to start the frontend from frontend terminal
Postgres password must be "postgres" and there should be a database with the name "testWad"
